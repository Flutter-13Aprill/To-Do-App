import 'package:flutter/material.dart';

class TaskEditValueContainer extends StatelessWidget {
  const TaskEditValueContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Task Edit Value"));
  }
}
